@page
@model TextInputModel
@{
    ViewData["Title"] = "Text Input";
}

@foreach (var inputPattern in Model.GovUkInputPatterns)
{
    <vc:gov-uk-text-input gov-uk-text-input-pattern=inputPattern></vc:gov-uk-text-input>
}